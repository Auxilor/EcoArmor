# `armor_set`
:::infoRequires:
EcoArmor
:::

Require a certain armor set

Works with `advance_armor` and `upgrade_armor_tier` triggers
# Filter Syntax
```yaml
filters:
  armor_set:
    - reaper
    - crystal
```
